# Email Configuration for Deadlock Detector
# This file is in the project directory
# Format: key=value (one per line)
# Lines starting with # are comments

email_to=ledinhnhuan1917@gmail.com
sender_name=Deadlock Detector System

# SMTP Configuration (for direct SMTP sending)
# If not set, will fallback to 'mail' command
# 
# For localhost SMTP (recommended):
# 1. Install postfix: sudo apt-get install postfix -y
# 2. Configure postfix (or use default settings)
# 3. Use localhost:25 (plain SMTP, no encryption needed)
#
smtp_server=localhost
smtp_port=25
from_email=detector@localhost

# Note: This uses plain SMTP on port 25 (no STARTTLS/TLS)
# For Gmail/Outlook, you need to configure local Postfix to relay emails
# or use the 'mail' command fallback method